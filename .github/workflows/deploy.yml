name: Build and Deploy Mule App

on:
  push:
    branches:
      - main

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Java
      uses: actions/setup-java@v3
      with:
        java-version: '8'

    - name: Set environment variables
      run: |
        echo "MAVEN_USERNAME=${{ secrets.MULE_USERNAME }}" >> $GITHUB_ENV
        echo "MAVEN_PASSWORD=${{ secrets.MULE_PASSWORD }}" >> $GITHUB_ENV
        echo "ENV=${{ secrets.MULE_ENV }}" >> $GITHUB_ENV

    - name: Build and Deploy with Maven
      run: mvn clean deploy -Dmule.username=${{ secrets.MULE_USERNAME }} -Dmule.password=${{ secrets.MULE_PASSWORD }} -Denv=${{ secrets.MULE_ENV }}
